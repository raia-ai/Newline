---
title: "Bank Control Utility"
source: "Bank Control Utility.docx"
tags: [Customer Documentation]
version: "1.0"
last_updated: "2025-11-17"
short_description: "This utility shows all data that has changed across a given period"
long_description: "This utility shows all data that has changed across a given period. New option looking at new date fields in bankrec (editdate and deletedate) so will only work from when clients get new version of exe that writes to these fields when data edited or deleted."
---

This utility shows all data that has changed across a given period. New option looking at new date fields in bankrec (editdate and deletedate) so will only work from when clients get new version of exe that writes to these fields when data edited or deleted.

You can export to csv and use filter and sort options in excel to pin-point issues